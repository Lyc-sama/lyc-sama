{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "g++ -Wall -g -lm -o main main.c binary_search.c in,pre,post,oder.c stack.c",
            
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}